<%= form_with(model: uttrance, local: true) do |form| %>
  <% if uttrance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(uttrance.errors.count, "error") %> prohibited this uttrance from being saved:</h2>

      <ul>
        <% uttrance.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :message %>
    <%= form.text_area :message %>
  </div>

<!--  <div class="field">-->
    <%#= form.label :confidence %>
    <%#= form.text_field :confidence %>
<!--  </div>-->

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
